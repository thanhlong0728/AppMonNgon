import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container : {
        marginRight : 5,
        flexDirection : 'row',
        alignItems : 'center',
    },
    iconText : {
        fontSize : 16,
        fontWeight : 'bold',
        marginLeft : 10
    }
})

export default styles;